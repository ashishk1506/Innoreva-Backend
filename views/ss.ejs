<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
   
    <title>Document</title>
    <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js" integrity="sha384-b5kHyXgcpbZJO/tY9Ul7kGkf1S0CWuKcCD38l8YkeH8z8QjE0GmW1gYU5S9FOnJ0" crossorigin="anonymous"></script>
    
</head>
<body>
    <!-- <form method="POST" action="/convert" >
        <input type="text" name="word" id="word" onchange="getValue()">
        <button type="submit">convert</button>
    </form>
  <div>
      <% text_new.map((array,index)=>{%>

        <% if(array.status == 0){ %>
            <%= array.hindi %>
        <% } else{ %>
           <% var bod = 23 %>
            <span type="button"  class="red-tooltip" data-toggle="tooltip" data-placement="bottom" title=<%=array.urdu%> style="text-decoration: underline; text-decoration-color: red;">
              <span onclick="valueChanger()" id="he" > <%= array.hindi %></span>
            </span>
        <% } %>

      <%})%> 

  </div> -->

 <a href="/user_controller">user_controller</a>
</body>
<script>
    var data
    function store(){
        data = document.getElementById('word').value
        localStorage.setItem('text',data)
    }
    
    document.getElementById('word').value = localStorage.getItem('storeValue')

    function getValue()
    {
        storeValue = document.getElementById('word').value
        localStorage.setItem('storeValue',storeValue)
    }
  
    $(document).ready(function(){
  $('[data-toggle="tooltip"]').tooltip();   
});
</script>

</html>